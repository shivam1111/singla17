<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<!-- Brokerage Report Wizard -->
    <record id="view_partner_mill_purchase_form" model="ir.ui.view">
            <field name="name">res.partner.mill.purchase.order.form.inherit</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
				<xpath expr = "//field[@name = 'category_id']" position = "after">
					<field name = "is_broker" />
				</xpath>
                <xpath expr = "//sheet/div[@name = 'button_box']" position = 'inside' >
                    <button class="oe_stat_button" name="print_brokerage_report" string="Print Brokerage" type="object" icon="fa-print" />
                </xpath>
            </field>
    </record>

    <record id="action_contacts_broker" model="ir.actions.act_window">
        <field name="name">Brokers</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,tree,form,activity</field>
        <field name="search_view_id" ref="base.view_res_partner_filter"/>
        <field name="context">{'default_is_broker': True}</field>
        <field name = "domain">[('is_broker','=',True)]</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Create a Contact in your address book
          </p><p>
            Odoo helps you track all activities related to your contacts.
          </p>
        </field>
    </record>

    <menuitem id="res_partner_menu_contacts_broker"
        name="Brokers"
        action="action_contacts_broker"
        parent="mill_purchase_order.menu_purchase_management"
        sequence="2"/>

</odoo>